# Session log — 2026-09-25

Everything shipped in this session, what's still open, and the invoice wording. All commits are
on `main` and live on beachhousemoving.xyz (lint, types, self-check, and the live pages were
checked at wrap-up: every key page returns 200, and no `$165` remains).

## Shipped (commits oldest → newest)

| Commit | What |
|---|---|
| `927ed25` | Reviews page 11 → 14: added Meredith Coughlin, Kole Prym, Phyllis Flowers to `TESTIMONIALS`. Archived all reviews and replies with the house style in `docs/REVIEW-RESPONSES.md` |
| `621615a` | Status audit + $300 → $400 → $500 retainer path (`docs/AUDIT-2026-09-25.md`); Places API billing TODO |
| `f212acc` | Lead capture: HTML-escaped owner emails; honeypot; **Resend `{error}` now checked** (failed sends used to show "thank you" and silently lose the lead); "Where this lead came from" block (first-touch + `bhm_src=gbp` cookie on the GBP 301); home size + "how did you hear" fields; contact form counts as a lead; addresses no longer sent to GA4; private Blob lead backup; privacy policy updated; `scripts/check-leads.mts` |
| `4b55e02` | Weekly analytics pull prompt + runner (`scripts/weekly-analytics/`) |
| `266ffbf` | "We reply to every request within 24 hours" (owner) on both forms, thank-you page, quote FAQ; redeploy switched Blob backup on |
| `1725c23` | Trust-strip phone link now tracked like every other tel link |
| `bebb2dd` | Docs for GTM v4 / GA4 dimensions; weekly pull reads them |
| `7ac34ed` | **Rate $195/hr for 2 movers + truck, plus drive time** (was $165). Updated only where the rate already appeared: /pricing, cost FAQ, Offer schema, cost post (ranges recalculated). Single source: `PRICING` + `RATE_LINE` |
| `c5fe73d` | "It's always best to call (850) 842-1962 for a more accurate quote" next to every rate mention (callout box on /pricing with tracked link `pricing-rate`). Single source: `CALL_FOR_QUOTE` |

## Done outside the repo

- **GBP:** replied to Meredith Coughlin's 5★ review (Travis posted).
- **Vercel:** private Blob store `bhm-leads` (store_md0DgROwHni9Oibd, iad1), connected to
  Production + Preview with `BLOB_STORE_ID` + `BLOB_READ_WRITE_TOKEN`.
- **GTM container version 4** "Lead source + phone location, 2026-09-25" (published):
  4 Data Layer Variables; `generate_lead` sends event_category / move_type / home_size (contact
  leads were mislabeled `quote_form` before); `phone_call_click` fires on the site's `phone_click`
  event with `phone_click_location` (old tel:-link trigger removed, so no double counting). Live `gtm.js`
  verified.
- **GA4:** `phone_call_click` marked as a key event (`generate_lead` already was); custom dimensions
  Move type, Home size, Lead form, Phone tap location.

## Still open

| Who | Item |
|---|---|
| Travis | **Test lead from a phone**: tap the mobile call bar, then send a quote named "TEST – Travis". Confirm the owner email has the source block, the Blob has a file under `leads/2026-09/`, and GA4 Realtime shows both events |
| Travis | Google Cloud billing + 100/day cap on the Places API (live reviews are down) |
| Travis | GSC request indexing: miramar-beach, santa-rosa-beach, niceville, sandestin |
| Travis | GBP: add the 3 new services, upload the 7 squares, repost post 1 (`~/Downloads/bhm-image-batch-sep08/gbp`) |
| Owner | 3+ mover rates · how drive time is counted · is fuel really included (C1–C2 in the pricing plan) |
| Travis | Bing Places: support ticket vs. delete-and-recreate |
| Claude | Weekly pull, Mondays. The in-session schedule ends with this session, so say **"run the BHM weekly pull"**, or install the cron line in `scripts/weekly-analytics/run.sh`'s header |
| Both | 11/01: first monthly lead report + notice of $400 from 12/01 (gate: calls ≥ August) |

## October $300 invoice: work to list

1. Review management: new review replied to; all 14 reviews + replies archived with a house style.
2. Reviews page brought current (14); live-reviews outage diagnosed (Google Cloud billing).
3. Full status audit: GSC, GA4, GBP. GBP calls 2 in Aug → 8 in Sept; homepage ~46 → 5.4.
4. Quote-form security: customer input sanitized, spam-bot trap.
5. Lost-lead bug fixed: failed emails now tell the customer to call instead of a false "thank you".
6. Lead source in every quote email (Google listing / search / AI / Facebook / direct).
7. Quote form: home size, "how did you hear about us", 24-hour reply promise.
8. Secure backup copy of every lead.
9. Analytics rebuild: contact leads counted, phone taps by button, no addresses in GA4 (Google
   policy), move type + home size reporting.
10. New starting rate published ($195/hr, 2 movers, plus drive time) with a clear "call for an accurate
    quote" everywhere it appears.
11. Weekly analytics report with recommendations.
